<!DOCTYPE html>
<html lang="fr">
    <head>
        <title>Artist details | Symfy</title>
        <link href="{{ asset('styles/app.css') }}" rel="stylesheet"/>
    </head>
    <body>
        {{ component('TopMenu') }}

        <div class="container">
            <div class="container-card">
                <img class="background" src="{{ artist.pictureLink }}" alt="image artist {{ artist.name }}">
                <h1> {{ artist.name }} </h1>
                <p> 🔥 Popularity: {{ artist.popularity }}/100 </p>

                <div class="action-div">
                    <a class="rounded-btn" href="{{ path((artist.isFavorite ? "app_favorite_remove_artist" : "app_favorite_add_artist"), {"id": artist.id}) }}">
                        {{ artist.isFavorite ? "🗑 Remove from" : "⭐️ Add to" }} favorites !
                    </a>
                </div>
            </div>
        </div>
    </body>
</html>