<!DOCTYPE html>
<html lang="fr">
    <head>
        <title>Détails</title>
        <link href="{{ asset('styles/app.css') }}" rel="stylesheet"/>
    </head>
    <body>
        <div class="container">
            <div class="container-card">
                <img class="background" src="{{ artist.pictureLink }}" alt="image titre {{ artist.name }}">
                <h1> {{ artist.name }} </h1>
                <p> 🔥 Popularité : {{ artist.popularity }}/100 </p>

                <a href="{{ path((artist.isFavorite ? "app_favorite_remove_artist" : "app_favorite_add_artist"), {"id": artist.id}) }}">
                    {{ artist.isFavorite ? "🗑 Supprimer des" : "⭐️ Ajouter aux" }} favoris !
                </a>
            </div>
        </div>
    </body>
</html>